name: sddomain
description: A domain layer for secret diary front end project.
version: 0.0.1
author: bohdan.martynov

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.26.0
  async: ^2.4.0
  logger: ^1.0.0

  # firebase
  firebase_auth: ^1.0.3
  firebase_core: ^1.0.3
  cloud_firestore: ^1.0.4

  sd_base:
    path:
      ../sd_base

  common_ui:
    path:
      ../common_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
